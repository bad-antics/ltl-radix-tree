// examples/demo.ltl — runnable demo for ltl_radix_tree

import std::io
use ltl_radix_tree as lib

fn main() {
    io::println("ltl_radix_tree demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}